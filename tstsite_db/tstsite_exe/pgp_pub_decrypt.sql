create or replace function tstsite_exe.pgp_pub_decrypt(bytea, bytea) returns text
    immutable
    strict
    security definer
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function tstsite_exe.pgp_pub_decrypt(bytea, bytea) owner to tstsite;

create or replace function tstsite_exe.pgp_pub_decrypt(bytea, bytea, text) returns text
    immutable
    strict
    security definer
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function tstsite_exe.pgp_pub_decrypt(bytea, bytea, text) owner to tstsite;

create or replace function tstsite_exe.pgp_pub_decrypt(bytea, bytea, text, text) returns text
    immutable
    strict
    security definer
    parallel safe
    language c
as
$$
begin
-- missing source code
end;
$$;

alter function tstsite_exe.pgp_pub_decrypt(bytea, bytea, text, text) owner to tstsite;

